import styled from 'styled-components';

export const PillContainer = styled.div`
  .test {
    padding: 4px 14px;
    font-size: var(--fontBodyTiny);
    background: var(--pink);
    color: var(--offWhite);
    width: 50px;
    min-with: 500px;
  }
`;
